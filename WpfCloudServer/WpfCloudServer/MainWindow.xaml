<Window x:Class="WpfCloudServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mini Cloud Server (WPF)"
        Height="450"
        Width="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="0"
                    Margin="0,0,0,10">

            <TextBlock Text="Port:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>

            <TextBox x:Name="txtPort"
                     Width="80"
                     Text="8888"
                     VerticalContentAlignment="Center"
                     Padding="5"/>

            <Button x:Name="btnStart"
                    Content="START SERVER"
                    Margin="10,0,0,0"
                    Padding="10,5"
                    Click="btnStart_Click"/>

            <Button x:Name="btnStop"
                    Content="STOP SERVER"
                    Margin="10,0,0,0"
                    Padding="10,5"
                    IsEnabled="False"
                    Click="btnStop_Click"/>
        </StackPanel>

        <TextBlock x:Name="lblStatus"
                   Grid.Row="1"
                   Text="Save Path: ..."
                   Foreground="Blue"
                   Margin="0,0,0,5"/>

        <ScrollViewer Grid.Row="2"
                      Background="Black">
            <TextBlock x:Name="txtLog"
                       Foreground="Lime"
                       FontFamily="Consolas"
                       TextWrapping="Wrap"
                       Padding="5"/>
        </ScrollViewer>
    </Grid>
</Window>
